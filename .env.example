# Example environment configuration

# Select which profile row in config/models.xlsx to use
APP_PROFILE=dev

# Optional override to point to a specific config file (YAML or XLSX)
# APP_MODELS_CONFIG=/absolute/path/to/models.xlsx

# Provider credentials referenced from config/models.xlsx when token_source=env
# (Remove or rename if unused.)
OPENAI_API_KEY=sk-...
OPENAI_API_BASE=https://api.openai.com/v1
GOOGLE_API_KEY=ux-...
XAI_API_KEY=xai-...
DASHSCOPE_API_KEY=dash-...

# Optional: Fernet encryption key for storing secrets
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
APP_KMS_KEY=
